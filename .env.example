# Optional overrides for Electron download mirrors (used by scripts/postinstall.cjs).
# Copy to .env and edit, or set these as system environment variables.
# ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
# ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/
